import { NextRequest } from "next/server";

export async function middleware(req: NextRequest) {
    // Get user details from cookie in jwt
    // Ensure that you decode jwt
    // Set user header to the user's j lwt
}